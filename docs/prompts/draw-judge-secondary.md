# 二次講評プロンプト（廃止メモ）

このファイルは過去の二次講評設計のメモです。現行の本番実装では二次講評を廃止し、一次採点のみで結果を返します。

## 現行方針
- 一次採点: OpenAI GPT-4.1 mini
- 二次講評: 実行しない
- フロント: 一次結果のみを表示し、二次ポーリングは行わない

## 補足
- 過去の比較実験や設計検討のためにファイルは残している
- 再導入する場合は `docs/architecture.yaml` と draw backend 実装を同時に更新する
